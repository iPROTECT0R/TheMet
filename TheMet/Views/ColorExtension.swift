// ColorExtensions
// Created by Raymond Shelton on 9/26/24.
// This file extends the Color struct to include custom colors used in the app.

import SwiftUI

extension Color {
    // Define custom colors for the app
    static let metBackground = Color("met-background") // Background color for the app
    static let metForeground = Color("met-foreground") // Foreground color for text and elements
}
